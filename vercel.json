{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --omit=dev",
  "buildCommand": "mkdir -p dist && printf '%s\\n' '<!doctype html><meta charset=\"utf-8\"><title>Guru Intelligence Proxy</title>' > dist/index.html",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/proxy.js?path=$1"
    }
  ]
}
