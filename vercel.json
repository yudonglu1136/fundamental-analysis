{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install --omit=dev",
  "buildCommand": "echo 'Using Vercel proxy function for the AWS-hosted Flutter app'",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/proxy.js?path=$1"
    }
  ]
}
