# The composition worker has a 90-second hard deadline. Cold PIT factor runs
# can exceed nginx's 60-second default before returning their first byte.
# Server-context inheritance keeps the generated application location and the
# separate public /api/internal deny rule intact.
proxy_read_timeout 120s;
