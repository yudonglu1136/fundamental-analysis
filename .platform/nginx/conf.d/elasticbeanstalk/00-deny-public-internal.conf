# Internal maintenance routes are available only by connecting directly to the
# Node listener on loopback. Public requests terminate at this nginx layer and
# must never reach Express, even if they carry a valid bearer credential.
location ~* ^/api/internal(?:/|$) {
    return 404;
}
